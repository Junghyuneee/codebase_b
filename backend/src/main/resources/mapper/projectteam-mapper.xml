<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.projectteam-mapper">

 <!-- 특정 ID의 ProjectTeam 조회 -->
    <select id="getProjectTeamById" parameterType="integer" resultType="com.example.dto.ProjectTeamDTO">
        SELECT 
            pjt_id AS pjtId,
            pjtname,
            pjtowner,
            pjtimg,
            pjtdescription,
            pjcategory
        FROM 
            project_team
        WHERE 
            pjt_id = #{id}
    </select>

    <!-- 모든 프로젝트 조회 -->
    <select id="getAllProjectTeams" resultType="com.example.dto.ProjectTeamDTO">
        SELECT 
            pjt_id AS pjtId,
            pjtname,
            pjtowner,
            pjtimg,
            pjtdescription,
            pjcategory
        FROM 
            project_team
    </select>

    <!-- 프로젝트 삽입 -->
    <insert id="insertProjectTeam" parameterType="com.example.dto.ProjectTeamDTO">
        INSERT INTO project_team (
            pjtname, 
            pjtowner, 
            pjtimg, 
            pjtdescription, 
            pjcategory
        ) VALUES (
            #{pjtname}, 
            #{pjtowner}, 
            #{pjtimg}, 
            #{pjtdescription}, 
            #{pjcategory}
        )
    </insert>

    <!-- 프로젝트 업데이트 -->
    <update id="updateProjectTeam" parameterType="com.example.dto.ProjectTeamDTO">
        UPDATE project_team
        SET 
            pjtname = #{pjtname},
            pjtowner = #{pjtowner},
            pjtimg = #{pjtimg},
            pjtdescription = #{pjtdescription},
            pjcategory = #{pjcategory}
        WHERE 
            pjt_id = #{pjtId}
    </update>

    <!-- 프로젝트 삭제 -->
    <delete id="deleteProjectTeam" parameterType="integer">
        DELETE FROM project_team WHERE pjt_id = #{id}
    </delete>

</mapper>

