<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.codebase.backend.review.mapper.ReviewCommentMapper">

<!-- 댓글 추가 -->
<insert id="addComment" parameterType="com.codebase.backend.review.dto.ReviewComment">
	INSERT INTO review_comments (review_id, content, created_date)
	VALUES (#{reviewId}, #{content}, NOW());
</insert>

<!-- 특정 리뷰의 댓글 가져오기 -->
<select id="getCommentsByReviewId" parameterType="int" resultType="com.codebase.backend.review.dto.ReviewComment">
	SELECT * FROM review_comments WHERE review_id = #{reviewId} ORDER BY created_date DESC;
</select>

</mapper>